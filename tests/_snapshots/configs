#pretty
{
  args = {
    profiles = {
      primary = "guangtao";
    };
    systems = {
      default = "x86_64-linux";
    };
  };
  exports = {
    summary = {
      hasHomeManager = true;
      nixosHasModules = false;
      nixosIsServer = true;
      profile = "guangtao";
      recipeKinds = [
        "home-manager"
        "nixos"
      ];
      system = "x86_64-linux";
    };
  };
  recipes = {
    home-manager = {
      modules = [
        "shell"
        "git"
      ];
    };
    nixos = {
      roles = {
        server = true;
      };
    };
  };
}