#pretty
{
  initFlake = {
    inputNames = [
      "flake-compat"
      "nixlib"
    ];
    outputNames = [
      "inputs"
    ];
  };
  inputs = {
    deSystemized = [
      "custom"
      "flake-compat"
      "nixlib"
      "nixpkgs"
    ];
    metadata = {
      hasCustom = true;
      hasNixlibFunc = true;
      hasNixpkgs = true;
    };
    systemized = [
      "custom"
      "flake-compat"
      "nixlib"
      "nixpkgs"
    ];
  };
  outputs = {
    linux = {
      metadata = {
        hasCustom = true;
        hasFunc = true;
      };
      packageName = "hello";
      packageSystems = [
        "x86_64-linux"
      ];
    };
    mergedSystems = [
      "x86_64-darwin"
      "x86_64-linux"
    ];
  };
}